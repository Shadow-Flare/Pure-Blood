event_inherited();

enum enemyActionType {animation, attack, damageZone};
enum enemyActionData {animation, duration, moveX, moveY, moveBurstX, moveBurstY, moveStart, moveDuration, performSoundID, hitSoundID, frameData, follow, hitStart, hitDuration, hitEffects, hitAngle, damageData, pierce};

flyingDefault = false;